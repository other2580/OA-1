<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="oa.dao.EmployeeDao">
	<!-- 添加新员工 -->
	<insert id="add" parameterType="Employee" keyProperty="id" useGeneratedKeys="true">
		INSERT INTO Employee(Nr,PASSWORD,NAME,Gender,DepartmentId,IsManager,STATUS) VALUES(#{nr}, #{password}, #{name}, #{gender}, #{department.id}, #{isManager}, #{status})
	</insert>
	<!-- 删除员工 -->
	<delete id="delete" parameterType="int">
		DELETE FROM Employee WHERE id = #{id}
	</delete>
	<!-- 修改员工信息 -->
	<update id="update" parameterType="Employee">
		UPDATE Employee
		<set>
			<if test="nr != null">nr = #{nr},</if>
			<if test="password != null">password = #{password},</if>
			<if test="name != null">name = #{name},</if>
			<if test="email != null">email = #{email},</if>
			<if test="gender != null">gender = #{gender},</if>
			<if test="department != null">DepartmentId = #{department.id},</if>
			<if test="isManager != null">IsManager = #{isManager},</if>
			<if test="status != null">status = #{status},</if>
		</set>
		<where>
			id = #{id}
		</where>
	</update>
	
	<sql id="sql">
		SELECT e.*,d.name AS 'department_name',
		 d.description AS 'department_description',
		 d.status AS 'department_status' 
		 FROM Employee AS e
			LEFT JOIN Department AS d ON(e.DepartmentId = d.id)
	</sql>
	
	<resultMap type="Employee" id="RMEmployee">
		<id column="id" property="id"/>
		<result column="nr" property="nr"/>
		<result column="password" property="password"/>
		<result column="name" property="name"/>
		<result column="email" property="email"/>
		<result column="gender" property="gender"/>
		<result column="isManager" property="isManager"/>
		<result column="status" property="status"/>
		<association property="department" javaType="Department">
			<id column="departmentId" property="id"/>
			<result column="department_name" property="name"/>
			<result column="department_description" property="description"/>
			<result column="department_status" property="status"/>
		</association>
		<!-- <association property="department" column="departmentId" 
			javaType="Department" select="oa.dao.DepartmentDao.fetchDepartmentById" /> -->
	</resultMap>
	
	<!-- 根据员工 Id查找员工 -->
	<select id="fetchEmployeeById" resultMap="RMEmployee" parameterType="int">
		<include refid="sql" /> WHERE e.id = #{id}
	</select>
	<!-- 根据员工 编号查找员工 -->
	<select id="fetchEmployeeByNr" resultMap="RMEmployee" parameterType="string">
		<include refid="sql" /> WHERE nr = #{nr}
	</select>
	<!-- 获取所有员工信息 -->
	<select id="fetchAllEmployee" resultMap="RMEmployee">
		<include refid="sql" />
	</select>
</mapper>